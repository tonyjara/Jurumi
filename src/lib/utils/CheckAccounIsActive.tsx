import React, { useEffect } from 'react';
import { trpcClient } from './trpcClient';

const CheckAccounIsActive = () => {
  //     const { data } = trpcClient.account.getUnique.useQuery();
  //   useEffect(() => {
  //     console.log(data);
  //     console.log('Hi i should only run once');

  //     return () => {};
  //   }, []);
  return <></>;
};

export default CheckAccounIsActive;
